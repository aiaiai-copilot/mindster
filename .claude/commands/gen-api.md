---
description: Generate API client from OpenAPI specification
---

Generate API client from OpenAPI specification

```bash
bun run gen:api
```

This regenerates TypeScript types and React Query hooks from `apps/api/openapi.yaml`.

Run this after any changes to the OpenAPI spec.
